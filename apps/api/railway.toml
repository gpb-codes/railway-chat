[build]
builder = "nixpacks"

[deploy]
startCommand = "npx prisma generate && npx prisma db push && node dist/main"
healthcheckPath = "/health"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
