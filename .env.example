# Base de datos (Railway provee DATABASE_URL automáticamente)
DATABASE_URL=postgresql://user:pass@host:5432/db

# JWT
JWT_SECRET=cambia-esto-en-produccion-abc123xyz
JWT_EXPIRY=7d

# CORS
ALLOWED_ORIGINS=http://localhost:3000

# URLs internas (Railway reference variables)
# NEXT_PUBLIC_API_URL=${{api.RAILWAY_PUBLIC_DOMAIN}}
# NEXT_PUBLIC_WS_URL=${{ws-server.RAILWAY_PUBLIC_DOMAIN}}
